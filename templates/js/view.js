/**
* View: OBJECT_NAME
*/
( function( $, window, document, utilities ) {

	var OBJECT_NAME = Gillie.View.extend({

	});

	// Global Exposition
	window.App.Views.OBJECT_NAME = new OBJECT_NAME();



})( this.jQuery, this, this.document, this.App.Helpers.Utilities );

