/**
* Model: OBJECT_NAME
*/
( function( $, window, document, utilities ) {

	var OBJECT_NAME = Gillie.Model.extend({

			// Called on instantiation
			initialize: function() {

			}

			// Default attributes
		,	defaults: {

			}

	});	

	// Global Exposition
	window.App.Models.OBJECT_NAME = new OBJECT_NAME();



})( this.jQuery, this, this.document, this.App.Helpers.Utilities );

