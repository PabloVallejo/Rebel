/**
* Handler: OBJECT_NAME
*/
( function( $, window, document, view, controller, utilities ) {

	var OBJECT_NAME = Gillie.Handler.extend({

		// Bind events
		events: {

		}

	});

	// Global Exposition
	window.App.Handlers.OBJECT_NAME = new OBJECT_NAME();


})( this.jQuery, this, this.document, this.App.Views.OBJECT_NAME, this.App.Controllers.OBJECT_NAME, this.App.Helpers.Utilities );

